TYPESAFE_API_KEY=
# Optional: an absolute or relative path to a yt-dlp binary, when the one on
# your PATH is too old for YouTube's current signatures. Leave blank to use
# whatever `yt-dlp` resolves to.
YT_DLP=
